# Public, canonical origin; do not add a trailing slash.
NEXT_PUBLIC_APP_URL=https://example.com
# Server-only values. Supply real values per environment; never commit them.
DATABASE_URL=
REDIS_URL=
OTP_HMAC_SECRET=
