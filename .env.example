# Build-only source revision identifier. Supply with the build environment or
# Docker's --build-arg BUILD_ID=...; do not use a mutable release label.
BUILD_ID=
# Public, canonical origin; do not add a trailing slash.
NEXT_PUBLIC_APP_URL=https://example.com
# Server-only values. Supply real values per environment; never commit them.
DATABASE_URL=
REDIS_URL=
OTP_HMAC_SECRET=
SESSION_HMAC_SECRET=
# Owner-approved SMS provider contract. The sender POSTs { to, templateId,
# parameters: { code } } with a Bearer API key; without these the OTP route
# fail-closes with 503 and no code is issued.
SMS_PROVIDER_URL=
SMS_API_KEY=
SMS_TEMPLATE_ID=
